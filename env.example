# Docker deployment port configuration
# Host port (accessible from outside) - change this to use a different external port
HOST_PORT=5000

# Note: Container always uses port 5000 internally for simplicity
# Only change HOST_PORT to expose the service on a different external port

# Optional variables for advanced configuration
# PYTHONUNBUFFERED=1